Schema {
    reads: List<Struct> {
        read: Char        // base pair (length: X)
                                // char = 8 bit, could be 3 bit?

        probs: FixedSizeBinary<32>      // emission/transmission probabilities (8 per read)
                                      // Int32 correct to store 32-bit posit?
    }

    haplos: List<Char>                // base pairs (length: Y)
}
